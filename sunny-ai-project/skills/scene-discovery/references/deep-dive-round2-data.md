# 深度收集第 2 轮：数据现状快速评估

**触发时机**：第 1 轮用户提交后立即触发。
**目的**：快速判断数据基础是否支撑 AI 应用，避免推荐无法落地的场景。

## 必须执行内联问卷生成协议 技能

按「🧭 内联问卷生成协议」（见 `SKILL.md` 顶部）执行；差异参数如下，完成后必须调用飞书 MCP `sunny_ai__feishu_create_doc`（创建飞书云文档）：

```
topic: "工厂AI项目数据可行性快速评估（CRISP-DM业务理解与数据理解阶段）"
scenario: [W2/W3 锁定的具体 KPI 和场景 + 准备文档系统清单里相关的系统]
industry: 舜宇光学 精密光电制造
doc_name: "[厂区代码]-AI场景发现-数据现状-[今日日期]"
purpose: "快速判断数据基础是否支撑AI应用，避免推荐无法落地的场景"
```

## 收到模板后，发给用户

```
这是第 2/3 轮：数据可行性快速摸底。

这一步不需要很精确，目的是帮 AI 判断"数据够不够、质量行不行"。
有一半答案就够了——AI 会基于现有信息给出置信度说明。
准备文档第四节"现有数字化基础"已经列了你的核心系统，
这一轮主要是把【针对你选定场景的数据细节】补全：数据量、时间跨度、标签情况、访问权限。
```

## 等待用户提交后

确认完后，**立即**读 [deep-dive-round3-history.md](deep-dive-round3-history.md) 并按其中指令执行（此轮可选，先问用户是否做），不要等用户再次选择技能。
