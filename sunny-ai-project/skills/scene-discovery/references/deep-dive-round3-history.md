# 深度收集第 3 轮：改善历史总结（可选）

**触发时机**：第 2 轮用户提交后。**可选，但强烈推荐。**
**目的**：了解已尝试的改善措施及失效原因，提高 AI 场景推荐的差异化价值。

## 先问用户是否做

```
第 3 轮是可选的，但做了之后推荐精准度会明显提升。

原因：了解"你们之前尝试过什么、为什么没解决"，能帮 AI 避免推荐你们已经试过但无效的方向，
并找到那个"关键缺口"——之前的方法做不到、AI 却能做到的点。

要做吗？（10 分钟左右）
```

## 如果用户同意：执行内联问卷生成协议

按「🧭 内联问卷生成协议」（见 `SKILL.md` 顶部）执行；差异参数如下，完成后必须调用 `sunny_ai__feishu_create_doc`：

```
topic: "工厂改善历史回顾与PDCA根因分析，识别传统方法的边界和AI的独特价值点"
scenario: [前两轮 + 准备文档的全部背景；如果准备文档第 5.2 节已有内容，作为初始素材带入]
industry: 舜宇光学 精密光电制造
doc_name: "[厂区代码]-AI场景发现-改善历史-[今日日期]"
purpose: "了解已尝试的改善措施及失效原因，提高AI场景推荐的差异化价值"
```

## 如果用户跳过

记录为「改善历史：未提供（置信度：低，不影响主要分析）」。

## 等待用户提交（或确认跳过）后

进入 `references/scenario-map-output.md`。
