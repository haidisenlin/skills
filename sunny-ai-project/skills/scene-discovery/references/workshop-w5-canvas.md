# Workshop W5：AI Use Case Canvas — 30 秒电梯演讲（问卷式）

**时长**：约 10 分钟
**框架来源**：**Bernard Marr《Data Strategy》AI Use Case Template**——全球数据战略领域最常用的 1 页 AI 立项画布
**交互模式**：**问卷式** —— 按 `SKILL.md` 顶部「🧭 内联问卷生成协议」执行（内联 `deep_research` + `sunny_ai__feishu_create_doc`），用户填好贴回来
**触发时机**：`references/scenario-map-output.md` 的 Step B-4 输出推荐场景之后、衔接 scene-prediagnosis 之前

## 步骤 1：生成 Canvas 问卷 飞书问卷（内联协议）

⚡ **执行内联问卷生成协议**（见 `SKILL.md` 顶部「🧭 内联问卷生成协议」）。差异参数如下，执行完必须调用 `sunny_ai__feishu_create_doc` 并把返回链接发给用户：

```
topic: "Bernard Marr AI Use Case Canvas 标准结构（决策替代 / 数据现状 / 价值假设 / 风险担忧）+ 30 秒电梯演讲脚本模板"
scenario: [用户从 Top 3 推荐里选定的那个具体场景名 + 对应的 Sponsor + 预算来源 + 时间窗]
industry: 舜宇光学 精密光电制造
doc_name: "[厂区代码]-[姓名]-W5-UseCase画布-[今日日期]"
purpose: "把选定的 AI 场景包装成可以向 Sponsor 提案的 1 页 AI Use Case Canvas + 30 秒电梯演讲草稿，让用户带着可立即使用的立项材料离场"
```

**问卷结构建议（生成飞书内容时遵循）：**
- 章节 1：Canvas 4 个核心问题（决策替代 / 数据现状 / 价值假设 / 风险担忧）→ 全部 🟡 推荐填
- 章节 2：向 Sponsor 提案所需补充（希望申请的预算 / 资源 / PoC 时间窗）→ 🟡 推荐填
- 章节 3：风险缓解初步方案 → ⚪ 选填（AI 会在后续输出里自动补建议）

## 步骤 2：将 `sunny_ai__feishu_create_doc` 返回的飞书文档链接发给用户

前面加以下引导语：

```
进入 workshop 最后一个互动环节：把推荐场景变成一份可以向 sponsor 提案的画布 📋

刚才我推荐了 Top 3 场景，请你先在对话框里告诉我【你选哪一个最想推进】。

我会用 Bernard Marr 的 AI Use Case Canvas 帮你把它包装成 30 秒电梯演讲草稿，
让你回去之后能直接拿着它去找 {Sponsor} 立项。

选定后我就生成对应的飞书问卷——问卷里 4 个核心问题分别是：
  Canvas Q1：你想让 AI 帮你做什么决策 / 替代什么人工判断？
  Canvas Q2：这个判断现在有什么数据支撑？
  Canvas Q3：你预期 AI 比现在的方法好在哪？请用【数字】表达
  Canvas Q4：你担心什么风险？（列 1-2 个最大的）

预计 10 分钟。请告诉我你要选哪个场景。
```

**等用户告知选定场景 → 用选定场景的名称去执行内联问卷生成协议 → 返回文档链接。**

发链接时加：

```
━━━ 飞书文档 ━━━
{sunny_ai__feishu_create_doc 返回的文档链接}

📌 Canvas Q3 是核心——请用数字表达价值，不要写"提升效率"。
  ✅ 好例："节奏从 3 秒/张降到 0.3 秒"
  ✅ 好例："把假阳性从 15% 降到 3%"
  ✅ 好例："把人工筛简历时间从 8 小时/周降到 30 分钟/周"
```

## 步骤 3：等用户提交

- ① 用户粘贴飞书文档链接 → 调用 `sunny_ai__feishu_get_doc` 解析
- ② 用户直接粘贴文档内容 → 解析
- ③ 用户说"我直接口述"→ 降级为对话式（按 Canvas Q1~Q4 顺序问）

## 步骤 4：追问 + 输出 30 秒电梯演讲草稿

**追问规则**：
- 如果 Q2（数据现状）答"应该有数据吧"——追问："你能在 30 秒内告诉我数据存在哪个系统、有多少条吗？如果不能，回去后第一件事就是查清楚——这是 sponsor 一定会问的问题。"
- 如果 Q3 写的是"提升效率"这种非量化表述，追问："请给一个数字——从 X 变成 Y。"

基于用户回答 + 前面 Workshop 积累的 Sponsor / 预算 / 时间窗信息，输出 30 秒电梯演讲草稿：

```
👌 太好了，根据你的回答，我帮你把这个场景包装成了 30 秒电梯演讲。
你下次见到 Sponsor【{Sponsor}】时，就用这段开场：

═══════════════════════════════════════════
📋 AI Use Case Canvas — {场景名称}

【现状】
现在我们的 {流程描述}，{Q1 描述的人工判断}，
每{时间单位}消耗 {人力 / 资源量}。

【AI 介入】
我想引入 AI 来 {Q1 决策替代}。

【数据基础】
我们已经有 {Q2 数据描述}。

【预期价值】
{Q3 数字化价值}，按 {年化 / 月度} 计算约相当于 {经济价值估算}。

【主要风险】
{Q4 风险} —— 我打算通过 {AI 建议的缓解方案} 来降低这个风险。

【请求】
我想申请 {预算 / 资源 / 时间} 做一个 PoC，{时间窗} 内验证可行性。
═══════════════════════════════════════════

📌 你可以把这段话直接拷走、回去之后稍微改一下就能用。

到这里，整个 AI 场景发现 workshop 全部结束了。下一步是【场景预诊断】——
基于你的场景生成定制化的六维资料收集清单。要继续吗？
```

## 进入 `references/handoff-prediagnosis.md`
